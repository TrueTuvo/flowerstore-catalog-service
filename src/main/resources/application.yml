spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/catalogdb
    username: catalog_user
    password: catalog_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081